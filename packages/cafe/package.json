{
  "name": "@claudecafe/cafe",
  "version": "0.1.1",
  "description": "Claude Code plugin — Claude Café in one: persona on shift (vendored from the @claudecafe/maids cast at build time, /maid to switch) plus the liveliness layer (greeting, mood cue, per-turn time, /look).",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun build.ts"
  },
  "devDependencies": {
    "@claudecafe/maids": "workspace:*"
  },
  "license": "ISC"
}
