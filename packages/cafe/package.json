{
  "name": "@claudecafe/cafe",
  "version": "0.1.6",
  "description": "Claude Code plugin — Claude Café in one: persona on shift (vendored from the @claudecafe/maid-personas cast at build time, /maid to switch) plus the liveliness layer (greeting, mood cue, per-turn time, /look).",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun build.ts"
  },
  "devDependencies": {
    "@claudecafe/maid-personas": "workspace:*"
  },
  "license": "ISC"
}
